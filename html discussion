<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Campus Connect - Chat Étudiant</title>
  <style>
    :root{
      --bg:#0f1724;
      --card:#0b1220;
      --accent:#a855f7;
      --muted:#94a3b8;
      --me:#9333ea;
      --other:#111827;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, Arial;
    }
    *{box-sizing:border-box}
    html,body{height:100%;margin:0;background:linear-gradient(180deg,var(--bg),#1a1030);color:#e6eef6}
    .app{max-width:1000px;margin:20px auto;padding:20px;display:grid;grid-template-columns:300px 1fr;gap:18px}
    .sidebar{background:var(--card);border-radius:12px;padding:14px;box-shadow:0 6px 20px rgba(2,6,23,0.6)}
    .brand{display:flex;align-items:center;gap:10px;margin-bottom:14px;flex-direction:column;text-align:center}
    .brand img{width:120px;height:auto;margin-bottom:10px;border-radius:12px}
    .brand-text{font-size:18px;font-weight:700;color:#c084fc}
    .topic-list{display:flex;flex-direction:column;gap:8px;max-height:60vh;overflow:auto;margin-top:10px}
    .topic{padding:10px;border-radius:10px;cursor:pointer;background:rgba(255,255,255,0.02);text-align:center;font-weight:500;transition:0.2s}
    .topic:hover{background:rgba(255,255,255,0.05)}
    .topic.active{background:rgba(168,85,247,0.15);border:1px solid rgba(168,85,247,0.3)}
    .panel{display:flex;flex-direction:column;height:80vh;background:rgba(255,255,255,0.02);border-radius:12px;padding:10px}
    .header{padding:10px 12px;border-bottom:1px solid rgba(255,255,255,0.1)}
    .header h2{margin:0;font-size:18px;color:#c084fc}
    .messages{flex:1;overflow:auto;padding:18px;display:flex;flex-direction:column;gap:12px}
    .msg{max-width:75%;padding:10px 14px;border-radius:14px;word-wrap:break-word}
    .msg.me{margin-left:auto;background:linear-gradient(180deg,var(--me),#a855f7);color:#f3e8ff;border-bottom-right-radius:4px}
    .msg.other{background:rgba(255,255,255,0.03);border:1px solid rgba(255,255,255,0.02)}
    .meta-line{font-size:12px;color:var(--muted);margin-top:4px}
    .composer{display:flex;gap:8px;padding:10px;border-top:1px solid rgba(255,255,255,0.1)}
    .input{flex:1;background:transparent;border:1px solid rgba(255,255,255,0.2);padding:10px 12px;border-radius:10px;color:inherit}
    .btn{background:linear-gradient(90deg,var(--accent),#7c3aed);border:none;padding:10px 14px;border-radius:10px;color:#02121a;font-weight:600;cursor:pointer}
    .btn:active{transform:translateY(1px)}
    @media(max-width:880px){.app{grid-template-columns:1fr;}}
  </style>
</head>
<body>
  <div class="app">
    <aside class="sidebar">
      <div class="brand">
        <img src="./imagecompus.JPG" alt="Campus Connect logo">
        <div class="brand-text">CAMPUS CONNECT</div>
        <div style="font-size:13px;color:var(--muted)">Your University United</div>
      </div>
      <h3 style="margin:10px 0;color:#cbd5e1;font-size:15px">Sujets</h3>
      <div id="topics" class="topic-list">
        <div class="topic active" data-topic="programmation">Programmation</div>
        <div class="topic" data-topic="math">Mathématiques</div>
        <div class="topic" data-topic="examens">Examens</div>
      </div>
    </aside>

    <main class="panel">
      <div class="header">
        <h2 id="topicTitle">Programmation</h2>
      </div>
      <div class="messages" id="messages">
        <!-- Messages will be inserted here -->
      </div>
      <div class="composer">
        <input id="messageInput" class="input" placeholder="Écrire un message..." onkeypress="handleKey(event)">
        <button id="sendBtn" class="btn">Envoyer</button>
      </div>
    </main>
  </div>
</body>
</html>
